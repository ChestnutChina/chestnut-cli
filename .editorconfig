# 让使用不同编辑器的开发者在共同开发一个项目时,遵循编码规范(编码风格)，就可以使用EditorConfig插件
# 会在项目根目录新建一个.editorconfig文件并使用其中定义的编码风格
# https://github.com/editorconfig/editorconfig/wiki/EditorConfig-Properties

root = true

# charset：文件编码。可选值：latin1/utf-8/utf-16be/utf-16le
# indent_size: 缩进数量。可选值：整数/tab
# end_of_line：换行符格式。说明见Wiki：换行。可选值：lf/crlf/cr/auto
# indent_style: 缩进类型。可选值：space/tab
# insert_final_newline：是否在文件的最后插入一个空行。可选值：true/false
# trim_trailing_whitespace：是否删除行尾的空格。可选值：true/false

# 对所有文件生效
[*]
charset = utf-8
indent_size = 2
end_of_line = auto
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true

# 对后缀名为 md 的文件生效
[*.md]
trim_trailing_whitespace = false